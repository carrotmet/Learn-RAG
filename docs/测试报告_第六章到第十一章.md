# RAG 教学项目 第六章~第十一章 功能排查验证报告

> 测试时间: 2026-06-21  
> 测试范围: 第六章（多路召回）~ 第十一章（API 接口）  
> 测试脚本: `backend/src/hybrid/test_ch6_11.py`  
> 排查原则: 以检查为主，禁止大改代码

---

## 执行摘要

| 章节 | 功能 | 状态 | 说明 |
|------|------|------|------|
| 第六章 | 多路召回 (MultiRecall) | **✅ 已实现** | `hybrid/retrieval/multi_recall.py` |
| 第七章 | 意图识别 (IntentRouter) | **✅ 已实现** | `hybrid/retrieval/intent.py` |
| 第八章 | 问题完善 (Enrich) | **✅ 已实现** | `hybrid/retrieval/enrich.py` |
| 第九章 | 融合层 (Fusion) | **✅ 已实现** | `hybrid/retrieval/fusion.py` |
| 第十章 | LangGraph 编排 | **❌ 未实现** | `agent/graph.py` 仍为一阶段代码 |
| 第十一章 | API 接口 | **❌ 未实现** | `agent/app.py` 缺少 Hybrid 端点 |

**结论**: 第六章~第九章的核心模块已开发完毕，功能正常。第十章和第十一章的编排层和 API 接口尚未实现，需要补充。

---

## 第六章：多路召回 (MultiRecall)

### 实现状态：✅ 通过

**文件**: `backend/src/hybrid/retrieval/multi_recall.py`

**检查项**:
- ✅ 文件存在
- ✅ 模块可导入
- ✅ `MultiRecall` 类存在
- ✅ `recall()` 方法存在：支持多策略并行、多通道聚合
- ✅ `recall_by_config()` 方法存在：支持意图配置驱动
- ✅ 功能测试：可正常召回并返回结果

**接口验证**:
```python
recaller = MultiRecall(store, vector=VectorChannel(), fts=FTSChannel(), graph=GraphChannel())
results = recaller.recall("自指", strategies=["standard"], channels=["vector", "fts"], k=3)
# 返回: {"vector": [...], "fts": [...]}

results = recaller.recall_by_config("自指", config={
    "strategies": ["standard"],
    "weights": {"vector": 0.5, "fts": 0.3, "graph": 0.2},
    "mode": "hybrid",
    "k": 3
})
```

---

## 第七章：意图识别 (IntentRouter)

### 实现状态：✅ 通过

**文件**: `backend/src/hybrid/retrieval/intent.py`

**检查项**:
- ✅ 文件存在
- ✅ 模块可导入
- ✅ `IntentRouter` 类存在
- ✅ `recognize()` 方法存在：LLM 驱动 + 规则回退
- ✅ `get_retrieve_config()` 方法存在：子意图→策略配置映射
- ✅ 两层架构：第一层 `needs_retrieve` 判定，第二层子意图多选
- ✅ 多选混合：支持多个 `retrieve.*` 子意图同时命中
- ✅ 规则回退：LLM 失败时触发关键词正则匹配

**接口验证**:
```python
router = IntentRouter()
result = router.recognize("什么是自指？")
# 返回: {
#   "intents": [{"path": "retrieve.concept", "confidence": "high", ...}],
#   "primary_intent": "retrieve.concept",
#   "needs_retrieve": True
# }

config = IntentRouter.get_retrieve_config([{"path": "retrieve.concept"}])
# 返回: {"strategies": ["summary", "standard"], "weights": {...}, "mode": "hybrid"}
```

---

## 第八章：问题完善 (Enrich)

### 实现状态：✅ 通过

**文件**: `backend/src/hybrid/retrieval/enrich.py`

**检查项**:
- ✅ 文件存在
- ✅ 模块可导入
- ✅ `Enrich` 类存在
- ✅ `check_completeness()` 方法存在：完整度判断 + 规则回退
- ✅ `rewrite()` 方法存在：问题改写
- ✅ 规则回退：短文本/代词/半截句子检测
- ✅ LLM 回退：完整度判断失败时返回默认完整

**接口验证**:
```python
enrich = Enrich()
result = enrich.check_completeness("什么是自指？")
# 返回: {"complete": True, "reason": "...", "rewritten_query": "..."}

result = enrich.check_completeness("它")
# 返回: {"complete": False, "follow_up_question": "您提到的'它'是指什么？"}

rewritten = enrich.rewrite("自指是啥？")
# 返回: 改写后的查询字符串
```

---

## 第九章：融合层 (Fusion)

### 实现状态：✅ 通过

**文件**: `backend/src/hybrid/retrieval/fusion.py`

**检查项**:
- ✅ 文件存在
- ✅ 模块可导入
- ✅ `Fusion` 类存在
- ✅ `rrf()` 方法存在：RRF 加权融合
- ✅ `deduplicate_by_parent()` 方法存在：按 parent_chunk_id 去重
- ✅ 支持通道权重：`{"vector": 0.5, "fts": 0.3, "graph": 0.2}`
- ✅ 返回包含 `fusion_score` 和 `hit_channels`

**接口验证**:
```python
fusion = Fusion(weights={"vector": 0.5, "fts": 0.3, "graph": 0.2})
result_dict = {
    "vector": [{"parent_chunk_id": 1000001, "score": 0.8, ...}],
    "fts": [{"parent_chunk_id": 1000002, "score": 0.6, ...}]
}
fused = fusion.rrf(result_dict, top_k=5)
# 返回: [{"parent_chunk_id": ..., "fusion_score": 0.0049, "hit_channels": ["vector"], ...}]
```

---

## 第十章：LangGraph 编排

### 实现状态：❌ 未实现

**问题**: `agent/graph.py` 仍为一阶段代码，未接入 HybridRAG 链路

### 当前状态

```python
# backend/src/agent/graph.py（当前）
builder.add_node("index", index_node)      # 一阶段节点
builder.add_node("retrieve", retrieve_node) # 一阶段节点
builder.add_node("generate", generate_node)  # 一阶段节点

builder.add_edge(START, "index")
builder.add_edge("index", "retrieve")
builder.add_edge("retrieve", "generate")
builder.add_edge("generate", END)
```

### 缺失项

| 缺失内容 | 影响 | 建议实现 |
|---------|------|---------|
| `state.py` 缺少 enrich/intent/recall/fusion 字段 | 无法存储 HybridRAG 中间状态 | 扩展 `RAGState` |
| `graph.py` 缺少 `enrich_node` | 无法判断问题完整度 | 添加节点，调用 `Enrich.check_completeness()` |
| `graph.py` 缺少 `intent_node` | 无法识别意图 | 添加节点，调用 `IntentRouter.recognize()` |
| `graph.py` 缺少 `recall_node` | 无法多路召回 | 添加节点，调用 `MultiRecall.recall_by_config()` |
| `graph.py` 缺少 `fusion_node` | 无法融合结果 | 添加节点，调用 `Fusion.rrf()` |
| 缺少条件路由 | enrich 不完整时无法追问 | 添加 `add_conditional_edges` |

### 建议实现（最小改动）

```python
# agent/state.py 扩展
class RAGState(TypedDict):
    messages: Annotated[list, add_messages]
    question: str
    
    # Enrich 层
    enrich_complete: bool
    enrich_reason: str
    enrich_rewritten: str
    enrich_follow_up: str
    
    # Intent 层
    intents: list
    primary_intent: str
    needs_retrieve: bool
    retrieve_strategies: list
    retrieve_weights: dict
    retrieve_mode: str
    
    # 召回层
    recall_results: dict
    fused_results: list
    
    # 生成层
    answer: str
    retrieved_docs: list
    retrieval_latency: int
```

```python
# agent/graph.py 新节点（示例）
from hybrid.retrieval.enrich import Enrich
from hybrid.retrieval.intent import IntentRouter
from hybrid.retrieval.multi_recall import MultiRecall
from hybrid.retrieval.fusion import Fusion
from hybrid.document_store import DocumentStore

enricher = Enrich()
intent_router = IntentRouter()
recaller = MultiRecall(DocumentStore())

def enrich_node(state: RAGState):
    question = extract_last_question(state["messages"])
    result = enricher.check_completeness(question)
    return {
        "enrich_complete": result["complete"],
        "enrich_reason": result["reason"],
        "enrich_rewritten": result.get("rewritten_query", question),
        "enrich_follow_up": result.get("follow_up_question", ""),
    }

# ... 其他节点类似
```

---

## 第十一章：API 接口

### 实现状态：❌ 未实现

**问题**: `agent/app.py` 缺少所有 `/api/hybrid/*` 端点

### 当前状态

`agent/app.py` 只包含一阶段接口：
- `/api/upload` — 文档上传
- `/api/status` — 知识库状态
- `/api/collect/*` — 数据采集
- `/api/testset/*` — 测试集搭建
- `/api/export/*` — 数据导出

### 缺失项

| 端点 | 方法 | 功能 | 影响 |
|------|------|------|------|
| `/api/hybrid/intent` | POST | 获取意图识别结果 | 无法调试意图识别 |
| `/api/hybrid/enrich` | POST | 问题改写 | 无法调试 Enrich |
| `/api/hybrid/recall` | POST | 多路召回调试 | 无法调试召回 |
| `/runs/stream` | POST | 混合检索对话流式 | 前端无法接入 HybridRAG |

### 建议实现（最小改动）

```python
# agent/app.py 新增端点

@app.post("/api/hybrid/intent")
async def detect_intent(data: dict = Body(...)):
    from hybrid.retrieval.intent import IntentRouter
    result = IntentRouter().recognize(data["query"])
    return {
        "intents": result.get("intents", []),
        "primary_intent": result.get("primary_intent"),
        "needs_retrieve": result.get("needs_retrieve"),
        "retrieve_config": IntentRouter.get_retrieve_config(result.get("intents", [])) if result.get("needs_retrieve") else None,
    }

@app.post("/api/hybrid/enrich")
async def enrich_query(data: dict = Body(...)):
    from hybrid.retrieval.enrich import Enrich
    return Enrich().check_completeness(data["query"])

@app.post("/api/hybrid/recall")
async def debug_recall(data: dict = Body(...)):
    from hybrid.retrieval.multi_recall import MultiRecall
    from hybrid.document_store import DocumentStore
    results = MultiRecall(DocumentStore()).recall(
        data["query"],
        strategies=data.get("strategies", ["standard"]),
        mode=data.get("mode", "hybrid")
    )
    return {"recall_results": results}
```

---

## 数据层验证

### 数据库表结构检查

```sql
-- backend/data/rag_data.db

-- 核心表（已存在）
chunks              ✅ 19 条（2 个文档）
chunk_derivatives   ✅ 0 条（当前仅 standard 策略）
index_status        ✅ 57 条（19 × 3 通道）
doc_fts             ✅ 19 条

-- 采集表（已存在）
conversations       ✅ 存在
retrieval_logs      ✅ 存在
llm_calls           ✅ 存在
user_feedback       ✅ 存在
raw_data            ✅ 存在
processed_data      ✅ 存在
```

### 数据路径检查

```
backend/data/
├── rag_data.db          ✅ SQLite 主数据库
├── chroma_db_hybrid/    ✅ ChromaDB 向量数据
├── knowledge_graph.pkl  ✅ 图谱数据
└── test_rag_data.db     ✅ 测试数据库
```

> 数据路径已统一，无分裂问题。

---

## 建议优先级

| 优先级 | 章节 | 任务 | 工作量 | 依赖 |
|--------|------|------|--------|------|
| P1 | 第十章 | 扩展 `agent/state.py` 添加 HybridRAG 字段 | 小 | 无 |
| P1 | 第十章 | 添加 `enrich_node`, `intent_node` 到 `agent/graph.py` | 中 | state 扩展 |
| P2 | 第十章 | 添加 `recall_node`, `fusion_node` 和条件路由 | 中 | 上述完成 |
| P3 | 第十一章 | 添加 `/api/hybrid/intent`, `/api/hybrid/enrich` | 小 | 无 |
| P3 | 第十一章 | 添加 `/api/hybrid/recall` | 小 | 无 |
| P4 | 第十一章 | 改造 `/runs/stream` 支持策略覆盖 | 中 | graph 改造完成 |

---

## 附录：测试脚本

**位置**: `backend/src/hybrid/test_ch6_11.py`

**运行方式**:
```bash
cd RAG教学
source backend/venv/bin/activate
PYTHONPATH=backend/src python -m hybrid.test_ch6_11
```

**输出**: 控制台报告 + `docs/test_report_ch6_11.json`

---

*报告结束。第六章~第九章功能完整，第十章和第十一章需要补充实现。*
